import { handleActions } from 'redux-actions';

// ------------------------------------
// App Reducer
// ------------------------------------
// import {  } from '../constants/ActionTypes';
const AppManager = {
  /**
   * [CONST_NAME]: (state) => ({
   *   ...state,
   *   attr: attrValue
   * }),
   */
}

// ------------------------------------
// handleActions and define the default
// state for cooresponding reducers
// ------------------------------------
export default handleActions(Object.assign({}, AppManager), {

});